int getInput() {
    return 1; // TODO - Actually read input from user
}

void main(){
    int x = getInput();
    int y = getInput();
    int XplusY = x + y;
    int XdivY = getInput() / y;
    bool XeqY = x == y;
    bool XgtY = x > y;
    bool XltY = x < y;
    bool xneY = x != printi(7); // error mismatch

    if (x != y) {
        return;
    }

    x = getInput();
    y = getInput();

    XgtY = x >= y;
    XltY = x <= y;
    XgtY = x > y;
    XltY = x < y;

    XneY = not XeqY;
    XeqY = (not XgtY) and (not XltY);
    if (((not XgtY) and (not XltY)) or (not (not (not XeqY))))


}